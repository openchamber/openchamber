# Task: SS-001 — Import Store + Init State vào SessionSidebar

## Metadata
- Status: pending
- Estimate: 15m
- Depends on: ST-001

## Files to Modify
- `packages/ui/src/components/session/SessionSidebar.tsx` (MODIFY)

---

## Description

Thêm import và state initialization cần thiết vào đầu `SessionSidebar.tsx` để chuẩn bị cho các task SS-002, SS-003, SS-004. Task này chỉ thêm code, **không thay đổi logic hiện có**.

---

## Các thay đổi cần thêm

### 1. Import store

Thêm import vào phần imports của file (sau các store imports hiện tại):
```
import { useSessionFoldersStore } from '@/stores/useSessionFoldersStore';
```

### 2. Import component

Thêm import `SessionFolderItem` (sau khi UI-001 hoàn thành, cần import vào đây):
```
import { SessionFolderItem } from './SessionFolderItem';
```

> Lưu ý: SS-001 và UI-001 có thể làm parallel vì chúng tạo file riêng. Nhưng import SessionFolderItem vào SS-001 chỉ được thêm sau khi UI-001 done. Executor có thể để comment placeholder trước.

### 3. State initialization trong SessionSidebar component

Trong function `SessionSidebar`, thêm selector để lấy data từ `useSessionFoldersStore`:

```
// Cần lấy:
- getFoldersForProject: (projectId) => SessionFolder[]
- createFolder: action
- renameFolder: action  
- deleteFolder: action
- addSessionToFolder: action
- removeSessionFromFolder: action
- toggleFolderCollapse: action
- getSessionFolderId: (projectId, sessionId) => string | null
- cleanupSessions: action
- collapsedFolderIds: Set<string>
```

Pattern giống cách lấy từ `useSessionStore`:
```
const getFoldersForProject = useSessionFoldersStore((state) => state.getFoldersForProject);
const createFolder = useSessionFoldersStore((state) => state.createFolder);
// ... etc
const collapsedFolderIds = useSessionFoldersStore((state) => state.collapsedFolderIds);
```

### 4. Thêm local state

Thêm vào phần useState declarations của SessionSidebar:
```
const [renamingFolderId, setRenamingFolderId] = React.useState<string | null>(null);
const [renameFolderDraft, setRenameFolderDraft] = React.useState('');
```

### 5. Thêm storage key constant

Thêm vào cụm constants ở đầu file (dòng ~89-94, cùng chỗ với `PROJECT_COLLAPSE_STORAGE_KEY` etc.):
```
const SESSION_FOLDERS_KEY = 'oc.sessions.folders';  // để reference, không dùng trực tiếp (store tự handle)
```

> Thực ra constant này chỉ để documentation — store đã tự handle storage. Có thể bỏ qua nếu không cần.

---

## Verification

```bash
bun run type-check
```

File không được có TypeScript errors sau task này. Nếu có lỗi "module not found" cho `SessionFolderItem`, executor có thể comment dòng import tạm thời cho đến khi UI-001 hoàn thành.

---

**⚠️ KHÔNG VIẾT CODE THỰC TẾ Ở ĐÂY** — Executor agent sẽ implement.
