---
title: Install
description: Install ALIAS ADE for desktop, web, or VS Code.
---

# Install

ALIAS ADE has three main surfaces:

- desktop app for macOS
- CLI-hosted web app with installable PWA
- VS Code extension

## Prerequisite

Install [OpenCode](https://opencode.ai) first.

## Web + PWA

```bash
curl -fsSL https://raw.githubusercontent.com/alias-ade/alias-ade/main/scripts/install.sh | bash
alias-ade --ui-password be-creative-here
```

Then open the URL printed by the CLI (usually `http://localhost:3000`).

## Desktop

Download the latest desktop build from the GitHub releases page or the ALIAS ADE download page.

## VS Code

Install from the VS Code Marketplace and sign into your usual OpenCode workflow.
