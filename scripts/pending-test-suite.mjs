const scriptName = process.argv[2] || 'test:unknown';

console.error(`[openchamber] ${scriptName} is not implemented yet.`);
console.error('[openchamber] Add the corresponding test suite before using this script.');
process.exit(1);
