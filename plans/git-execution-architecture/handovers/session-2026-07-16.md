# Session Handover — 2026-07-16

## Current state

- Phase 2 and Phase 3 verification/review blockers are closed. Phase 4 final child-timeout, CLI-validation, and test-discovery hardening is complete, and both final-code full profiles pass with exact reviewed counts, zero timeouts/unexpected errors, and successful cleanup.
- No Git/GitHub commands, dependency installation, commits, pushes, UI work, or `../opencode` changes were performed.
- Completed-result caching remains blocked without authoritative external Git invalidation.
- Legacy `getWorktrees` empty-on-failure behavior is documented and intentionally unchanged pending a separate external-contract gate.

## Phase 3 discovery, decision, and closure

- Re-read root/project rules, all matching skills, VS Code module documentation/package scripts, and canonical Phase 1/2 artifacts.
- Exhaustive imports found only `bridge-git-runtime.ts` and `bridge-git-special-runtime.ts` directly consuming `gitService.ts`; this permits a package-local facade without an unsafe core call cycle.
- Rejected coordinating raw `execGit()` alone because the preferred built-in VS Code Repository API path would bypass the execution model. The facade must acquire one outer lease before core built-in/raw selection.
- Direct raw owners are bridge conflict details, filesystem `check-ignore`, and skills-catalog Git. `api:fs:exec`, hooks/helpers, external Git, `git --version`, and user-authored start commands remain explicit bypasses.
- Fast worktree attachment/bootstrap must receive separate post-topology admission; an outer wrapper alone would otherwise release while background Git continues.
- Canonical Phase 3 phase and implementation artifacts are present and marked complete.
- The baseline combined-test Bun module-mock leak was fixed through explicit specialized-runtime dependency injection, complete child-process mocks, and local filesystem dependency injection rather than by splitting or weakening validation.

## Phase 3 implementation

- Added package-local structured errors, bounded canonical context resolution, conflict-aware coordination, operation-local read scope, raw execution ownership, and a scheduled service facade under `packages/vscode/src/`.
- Machine-checks semantically reflect and classify all 51 imported `gitService.ts` function exports exactly once. A TypeScript compiler AST scan covers every direct extension-host child-process Git owner, with explicit bypasses inventoried separately.
- The facade acquires the outer lease before the core chooses VS Code's built-in Repository API or raw fallback. Structured overload/cancellation propagates, while ordinary PATH discovery failure receives a bounded canonical directory fallback so the built-in path can still run.
- Fast worktree attachment retains the initiating common identity and is admitted after the initiating topology operation. Bootstrap/reset work receives a separate common+target lease; unresolved bootstrap identity conservatively escalates to a topology barrier.
- Conflict-detail and filesystem `check-ignore` probes use scheduled raw reads. Skills-catalog clone keeps canonical destination ownership through fallback, local commands, copy, and cleanup while releasing scarce network capacity after clone.
- Status sharing is bounded, generation-aware, full/light aware, and in-flight only. `GIT_OPTIONAL_LOCKS=0` is scoped through `AsyncLocalStorage` only to genuine raw observations.
- Legacy clean/empty behavior, RuntimeAPI payloads, `BridgeResponse` envelopes, built-in-first behavior, raw fallback, and explicit shell/helper bypasses remain unchanged.
- Verification closure added a real dependency-injected facade suite covering outer admission, built-in/raw same-lease behavior, observation-only optional locks, full/light status sharing, post-parent worktree admission, structured errors, ordinary fallback, and executable delegation for all 51 operations.
- The strengthened pathological 30,000-caller fan-out fixture covers 200 common contexts and 300 worktrees and asserts exact completion, exactly 600 mutations, exactly 60 network operations, generation movement, resolver/coordinator cleanup, every configured bound, and no session-derived identities. It is not representative session concurrency; deterministic writer and topology fairness remain independently tested.
- Phase 4 now labels that synthetic fixture as a pathological 30,000-caller fan-out correctness guard. The requested target scale is separately modeled as 30,000 logical session entities mapped to 300 worktree identities; sessions must not multiply Git submissions.

## Phase 4 implementation and local validation

- Added `scripts/perf/git-execution.ts`, its focused executable test, harness-only TypeScript config, and package commands for `pr-real`, exact/default and reduced-development `target-real`, soak, cap sweep, tests, type-check, and lint.
- Real Git uses the web coordinator/resolver against disposable local repositories and bare remotes. The same deterministic fixture asserts VS Code coordinator/resolver parity without importing the built-in Git API outside Extension Host.
- Blocking checks include distinct entity/scenario/API/scheduled/Git counts, exact waiter/underlying latency sample counts, closed command equations, executable safety guards, coalescing, caps, fairness/conflicts, generations, errors, drain, bounds/eviction, FDs, child ownership, and cleanup. Latency values remain advisory.
- Reports are JSON on stdout by default; no report, `.git` fixture, log, trace, profile, or machine baseline may be committed. Explicit output must be one new canonical file outside the workspace.
- No dependency, production behavior, telemetry hook, CI workflow, or generated artifact is part of Phase 4.
- Every direct Git child is checked before spawn for fixture cwd/path operands, sanitized Git/credential environment, isolated system/global config, disabled prompts/hooks/auto-GC, and registered local-only remotes; guard counts/evidence are reported.
- Validation exposed and fixed two harness-only accounting defects: cap generations were initially read after eviction, and soak generations were lost when automatic idle pruning preceded sampling. Cap movement is now aggregated before each eviction; soak uses deliberate quiescent forced eviction and cumulative exact movement.

## Phase 2 findings previously closed

1. Added executable scan/install tests using the real clone coordinator and controlled Git results. They cover acquisition, preferred/fallback outcomes, global concurrent destinations, `releaseNetwork()` before destination-local work completes, structured errors, and temporary cleanup.
2. Fast-create now awaits the outer topology operation before admitting background attachment. Public results remain `pending`; deterministic real-Git tests observe `ready` and `failed` without deadlock/re-entry.
3. `validateWorktreeCreate` rethrows `GIT_EXECUTION_*` failures and preserves ordinary validation envelopes. Tests cover overload, queue timeout, re-entry, ordinary wrapper errors, and real unresolved-ref details.
4. `validateWorktreeCreateCore` is explicitly unscheduled inside the existing common/topology lease. Local probes reuse that lease; no nested read is acquired and no optional-lock override is applied to the compound mutation/network workflow.
5. Removed stale project-icon/FS Git-spawn composition arguments while retaining `spawn` for user-authored `/api/fs/exec`.
6. `getStats()` is side-effect-free. Throttled fake-clock coverage proves ordinary admission and drain paths still prune expired idle state without a perpetual timer.
7. `/api/fs/list` timeout coverage proves abort, fail-open unfiltered output, no unhandled rejection, and no unsettled injected work.
8. Canonical dead-code results now use the repository root scope.
9. Git, FS, skills-catalog, OpenCode composition, Phase 2 plan, implementation, todo, phase, and handover docs reflect implemented truth.

## Phase 2 validation retained

### Combined focused tests

```bash
bun test server/lib/git/context-resolver.test.js server/lib/git/execution-coordinator.test.js server/lib/git/operation-classification.test.js server/lib/git/service.test.js server/lib/git/routes.test.js server/lib/fs/routes.test.js server/lib/fs/search.test.js server/lib/skills-catalog/git.test.js server/lib/notifications/template-runtime.test.js server/lib/opencode/project-icon-routes.test.js
```

Result: 145 passed, 0 failed, 647 assertions across 10 files in 21.65s. Expected logs came from deliberate background-attachment failure and existing negative cherry-pick/revert tests.

### Runtime syntax

```bash
node --check server/lib/git/context-resolver.js && node --check server/lib/git/execution-coordinator.js && node --check server/lib/git/execution-errors.js && node --check server/lib/git/operation-classification.js && node --check server/lib/git/service.js && node --check server/lib/git/routes.js && node --check server/lib/fs/routes.js && node --check server/lib/fs/search.js && node --check server/lib/skills-catalog/git.js && node --check server/lib/skills-catalog/scan.js && node --check server/lib/skills-catalog/install.js && node --check server/lib/notifications/template-runtime.js && node --check server/lib/opencode/feature-routes-runtime.js && node --check server/lib/opencode/project-icon-routes.js && node --check server/index.js
```

Result: passed with no output.

### Web checks

```bash
bun run type-check
bun run lint
```

Result: both passed. Lint is configured for `src/**/*.{ts,tsx}`; server JavaScript is covered by focused tests and syntax checks.

### Root-scoped dead-code analysis

```bash
bunx --no-install knip@5.80.0 --no-exit-code --include files,exports,nsExports,types,nsTypes,enumMembers,duplicates
```

Result: 189 unused files, 304 unused exports, and 176 unused exported types. Knip warned that `--no-install` used a stale cached installation. The report remains a noisy non-blocking baseline.

## Phase 3 final verification

### Focused bridge/facade follow-up

```bash
bun test src/bridge-git-runtime.test.js src/bridge-git-special-runtime.test.js src/git-execution-service.test.ts
```

Result: 17 passed, 0 failed, 173 assertions. The isolated production-listener case passed independently with 1 test, 0 failures, and 7 assertions.

### VS Code package tests

```bash
bun test src
```

Result: 84 passed, 0 failed, 837 assertions across 18 files.

### Type-check and lint

```bash
bun run type-check
bun run lint
```

Result: both passed with no diagnostics. Type-check covers the extension and webview TypeScript projects.

### Production build

```bash
bun run build
```

Result: extension and webview production builds passed. Vite retained existing non-blocking KaTeX font-resolution, mixed static/dynamic import, and large-chunk warnings.

### Root-scoped dead-code analysis

```bash
bunx --no-install knip@5.80.0 --no-exit-code --include files,exports,nsExports,types,nsTypes,enumMembers,duplicates
```

Result: 189 unused files, 304 unused exports, 176 unused exported types, and five configuration hints. No new Phase 3 finding remained. Knip warned that `--no-install` used a stale cached installation.

### Conditional shared-web regression

Not run because Phase 3 changed no shared web file.

## Phase 4 initial verification (schema v1; superseded)

Independent testing after this validation found four harness/report-contract gaps. Phase 4 is reopened for caller-dimension separation, all-waiter fan-out latency, executable safety evidence, and closed exact Git-command accounting. The prior `/tmp` report is stale and must not be edited or reused as evidence.

### Harness and profiles

- `bun run test:perf:git`: 3 passed, 0 failed across one file.
- `bun run perf:git:pr-real`: passed in 679.397ms; 30 logical sessions, 8 submissions/operations, 31 Git commands, 8/8 generation movement, one expected lock failure/retry, zero unexpected errors, and complete drain/cleanup.
- `bun run perf:git:target-real:dev`: passed in 769.455ms; 600 logical sessions, 4 common directories, 6 worktree identities, 623 submissions, 29 underlying operations, 75 Git commands, and 34/34 generation movement.
- `bun run perf:git:cap-sweep`: passed in 847.579ms; 1,200 identical-fixture operations across caps 2/4/6/8/12, exact 800/800 generation movement, zero unexpected errors, and no production-default change.
- `bun run perf:git:soak -- --duration-ms 10500 --rate 2`: passed in 10730.271ms; 21 operations, two topology churn operations, one forced-idle eviction, exact 14/14 generation movement, and complete drain/cleanup. An earlier operator invocation with unsupported `--duration` was rejected by the CLI; the documented `--duration-ms` invocation above is authoritative.

### Static/package/documentation checks

- Harness type-check and lint passed with no diagnostics.
- Web coordinator test: 24 passed, 0 failed, 120 assertions. VS Code coordinator test: 14 passed, 0 failed, 321 assertions.
- Web and VS Code package type-check/lint pairs passed with no diagnostics.
- Documentation validation passed: 387 pages and 43 sidebar links.
- Root `bunx --no-install knip@5.80.0 ...` remained a noisy non-blocking baseline at 184 unused files, 303 unused exports, 175 unused exported types, and five configuration hints. No harness path was reported.
- Full default `target-real` and default five-minute soak were intentionally not run. No build was run because production/package output behavior did not change.

## Phase 4 schema v2 independent-review closure

- Removed aggregate `logicalCallers`; entity mapping now proves sessions → identities with zero API-submission/scheduled-operation/Git-command deltas. Scenario callers are declared independently and reported beside API submissions, scheduled operations, and Git commands.
- Added one exact waiter-observed total sample per API submission while retaining separate underlying queue/service/total samples. Full-target pathological fan-out now expects 30,000 waiter samples and 300 underlying samples.
- Replaced declarative safety booleans with executable guards for fixture paths, every child cwd/path contract, sanitized Git/GCM/askpass/SSH-agent environment, prompt/hook/auto-GC policy, registered local remotes, and new external output files. Every guard fails before spawn/write and reports counts/evidence; direct-child counts still exclude helpers/external processes.
- Added the closed Git command categories `environment`, `fixture-setup`, `discovery`, `workload`, `lock-recovery`, and `cleanup`. Every call site requires a category/class; exact category/class maps, totals, successes/failures, and equations block every profile.
- Focused harness: 6 passed, 0 failed in 2.27s. Harness type-check/lint passed; documentation validation passed for 387 pages and 43 sidebar links.
- `pr-real`: passed in 607.106ms; 30 → 2 → 0/0/0 entity mapping; 8 API submissions/8 scheduled operations/31 Git commands; 30 successes/1 expected lock failure; 31 checks for each child guard and zero guard failures.
- Reduced target: passed in 786.507ms; 600 → 6 → 0/0/0 mapping; startup 6/6/6/6; pathological fan-out 600/600/6/6; waiter/underlying samples 600/6; totals 623 API submissions, 29 scheduled operations, 75 Git commands.
- Cap sweep: passed in 831.788ms; 1,200 callers/submissions/scheduled operations, one environment Git command, and 800/800 generations.
- Short soak: passed in 10749.398ms; 21 callers/submissions/scheduled operations, 71 Git commands, 14/14 generations, two topology churn operations, and one idle eviction.
- The retained `/tmp` schema v1 report is stale and was not edited.
- Root no-install Knip remained at 184 unused files, 303 unused exports, 175 unused exported types, and five configuration hints, with the stale cached-install warning and no harness path reported. Web/VS Code package checks and builds were not rerun because production runtime source did not change.

## Phase 4 deterministic soak-wave correction

- **Symptom:** the flat 20-caller/second plan declared every status caller as one scheduled operation/Git command. The real coordinator instead coalesces same-worktree/shape/generation status callers only while the first task remains in flight, so the failed five-minute result varied with machine/Git duration.
- **Root cause and fix level:** this was a harness planner/expectation defect, not a production coordinator defect. One frozen plan is now built before fixture creation and drives execution, API/scheduled counts, generation movement, and the closed command equation.
- Status callers share only inside the same one-second wave, worktree, planned common/worktree generation, and idle segment. Every group's callers are submitted synchronously behind a per-worktree tail, guaranteeing one underlying task. Separate groups/local mutations remain ordered; fetch/topology operations replace every tail in their common context with one barrier; unrelated common contexts remain concurrent.
- The report's `details.immutablePlan` records caller types, group-size counts, event/status-group counts, exact API/scheduled/Git/generation expectations, category/class maps, and the equation. `inspectGitExecutionSoakPlan()` permits a no-Git hard exact default-plan regression.
- A programmatic-only `testHooks.soakStatusDelayPatternMs` seam varies underlying status duration without exposing manual grouping/timing controls through the CLI.
- Default seed 8755 now fixes 6,000 callers/API submissions, 3,279 status callers in 1,869 groups, 4,590 scheduled operations, 4,754 Git commands, and generation movement 3,116. Equation: `1 environment + 39 fixture-setup + 6 discovery + 4649 workload + 0 lock-recovery + 59 cleanup = 4754`.
- Focused harness passed 8/8 in 7.56s. Seed 8755 produced 48 API / 33 scheduled / 79 Git with 30 status callers in 15 groups under both normal and `[0, 75, 5, 120]ms` timing. Seed 4660 produced 48 API / 38 scheduled / 84 Git with 26 status callers in 16 groups under `[120, 0, 35, 5]ms` timing.
- Explicit profiles passed: `pr-real` 670.287ms/31 Git; reduced target 742.835ms/75 Git; cap sweep 869.287ms/one Git and generation 800; normal short soak 10681.433ms with 21 API, 20 scheduled, 70 Git, two topology operations, one idle eviction, and generation 14. The same short plan passed variable timing in 10748.303ms.
- Harness type-check/lint passed; docs validation passed for 387 pages and 43 sidebar links. Root no-install Knip retained the noisy 184-file/303-export/175-exported-type baseline plus five hints and no harness finding. No production scheduler/service, package script, dependency, CI workflow, generated artifact, or prior report changed.
- The earlier failed full-soak report remains non-passing, is superseded by the final corrected soak, and must not be edited, overwritten, or cited as successful evidence.

## Final Phase 4 full-profile evidence

- Target PASS: `/tmp/opencode/git-execution-target-real-final-hardening-59e77404-20260716-8c24e1b7.json` — 30,962 API submissions, 1,262 scheduled operations, 2,876 Git commands, 30,000/300 pathological fan-out waiter/underlying counts, generation movement 1,324, zero child timeouts, zero unexpected errors, and successful cleanup.
- Corrected default soak PASS: `/tmp/opencode/openchamber-git-soak-final-hardening-59e77404-20260716-5d91f3a8.json` — 6,000 API submissions, 4,590 scheduled operations, 4,754 Git commands, 3,279 status callers in 1,869 groups, generation movement 3,116, zero child timeouts, zero unexpected errors, and successful cleanup.
- These generated reports are local-only evidence, are not committed, and have non-durable paths. Their filenames are not required values for future runs or reusable PR-template fields.
- The earlier failed soak remains explicitly non-passing, is superseded by the final corrected soak, and is excluded from PR evidence.

## Phase 4 final harness hardening

- Added a 60,000ms default per-Git-child timeout, configurable as `--git-child-timeout-ms` and reported in profile config. Timing begins on `spawn` only.
- POSIX children run in dedicated process groups; timeout sends only that owned group `SIGTERM`, waits 1,000ms for actual `close`, then sends `SIGKILL` only if needed. Windows targets only the exact child. Timeout/termination/graceful/forced/reap counters prove metrics release after close.
- Timeout is an unexpected profile error. The command is counted as failed, active child/submission/operation state drains, and outer fixture cleanup still executes.
- Non-soak CLI use of `--duration-ms` or `--rate` is rejected before fixture creation. Focused/root tests use a bounded entrypoint that rejects full target and five-minute soak, while package-script inventory keeps the two full profiles explicit.
- Existing canonical output-boundary tests remain portable; no Windows symlink test was added because privilege/developer-mode requirements would make it non-portable.
- Final local validation: focused harness 12/12; `pr-real` PASS with 31 Git commands; short soak PASS with 21 API / 20 scheduled / 70 Git and generation 14. Both smoke profiles used the 60,000ms default with zero timeouts. Type-check/lint passed; docs validation passed 387 pages/43 links; no-install Knip stayed at 184/303/175 plus five hints.

## Phase 4 closure

The final-code target and corrected five-minute soak satisfy the remaining Phase 4 evidence gate. No generated report is committed, and no local artifact path is a future-run requirement. Publication remains outside this work package. Completed-result caching remains blocked until authoritative external Git invalidation exists.
